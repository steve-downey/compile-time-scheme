# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

name: smd.schemepoc
title: smd.schemepoc
version: ~

nav:
  - modules/ROOT/nav.adoc

ext:
  cpp-reference:
    config: docs/mrdocs.yml
    using-namespaces:
      - smd::kit::foundation
      - smd::cl::foundation
      - smd::cl::symbol
      - smd::cl::reader
      - smd::cl::core
      - smd::cl::elaborator
      - smd::cl::eval
