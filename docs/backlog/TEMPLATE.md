# BL-NNNN: short title

- **Status:** open | scheduled as step LNN | declined | superseded by BL-MMMM
- **Date:** YYYY-MM-DD
- **Origin:** where this came from — a plan doc, a step's discovery, a review, a question
- **Frozen-tree impact:** none | requires a semantic edit to `src/smd/smdscheme/**` (D1 — needs a divergence doc and orchestrator sign-off before scheduling)

## What

The change, in a few sentences. Name the files it would touch.

## Why it is not done

The reason this is backlog and not a step: unclear payoff, blocked on a
decision, blocked on a toolchain feature, or simply not yet worth the churn.

## Evidence

Measurements, file:line references, or compiler behaviour that establishes the
problem is real. Say how each number was obtained. Distinguish what was
verified from what is expected.

## Open questions

The things that must be settled before this can be written as a step. One per
line.

## Cost and risk

Rough size, what it would break, and what would have to be re-verified.

## Decision criteria

What would make this worth scheduling — or worth closing as declined.
