#+options: ':nil *:t -:t ::t <:t H:3 \n:nil ^:nil arch:headline author:t
#+options: broken-links:nil c:nil creator:nil d:(not "LOGBOOK") date:t e:t
#+options: email:nil f:t inline:t num:t p:nil pri:nil prop:nil stat:t tags:t
#+options: tasks:t tex:t timestamp:t title:t toc:nil todo:t |:t
#+options: html5-fancy:t
#+bibliography: references.bib
#+TITLE: Building a Compile-Time Scheme in C++26: Phase 8 - Visualizing Execution
#+AUTHOR: Steve Downey
#+DATE: 2026-05-29

#+begin_abstract
By lowering my Scheme AST into heavily typed ~std::execution~ Senders, I gained improved performance.
But I also created an introspective nightmare.
The resulting Sender AST is a large set of deeply nested template permutations.
How do I prove what the C++ compiler is actually building without deciphering thousands of lines of compiler error backtraces?
#+end_abstract

#+MACRO: TEASER_END
{{{TEASER_END}}}

* The Reflection Bridge

In Phase 8, I utilize C++26 Reflection (~std::meta::info~) alongside a custom ~string_writer~ Applicative [cite:@mcbride2008applicative] to project the generated execution typings into the Graphviz [cite:@graphviz_doc] dot-language for visual rendering.

C++26 Reflection (P2996) allows C++ programs to imperatively query the structural definitions of types during constant evaluation.
Instead of writing recursive template metaprogramming tricks with partial specializations, I write a straightforward loop that interacts via ~<meta>~ using the ~^^Sender~ reflection syntax.

* The Intermediate Representation: ~scheme_tree~

Before generating Graphviz output, the reflected sender topology is materialized into a homogeneous, ~consteval~-friendly tree.
Two types form this intermediate representation:

#+transclude: [[orgit:~/src/steve-downey/schemepoc/schemepoc/::src/smd/smdscheme/sender/scheme_node_data.hpp::c1d2e3f4-a5b6-4c7d-8e9f-0a1b2c3d4e5f]] :lines 2- :src cpp :end "c1d2e3f4-a5b6-4c7d-8e9f-0a1b2c3d4e5f end"

#+transclude: [[orgit:~/src/steve-downey/schemepoc/schemepoc/::src/smd/smdscheme/sender/scheme_tree.hpp::d2e3f4a5-b6c7-4d8e-9f0a-1b2c3d4e5f60]] :lines 2- :src cpp :end "d2e3f4a5-b6c7-4d8e-9f0a-1b2c3d4e5f60 end"

This is the same flat arena pattern as the datum and core trees from Phases 3 and 4: a ~static_vector~ of nodes addressed by integer index, trivially copyable, and safe to produce in a ~consteval~ context.
The difference is that the recursion depth is bounded by the sender composition depth rather than the Scheme source nesting depth.

* Building the Tree: Reflection Over Sender Types

~build_scheme_tree~ populates a ~scheme_tree~ by walking the sender type at ~consteval~ time:

#+transclude: [[orgit:~/src/steve-downey/schemepoc/schemepoc/::src/smd/smdscheme/sender/build_scheme_tree.hpp::ad3fee60-a0ea-4464-966c-bae3a964db79]] :lines 2- :src cpp :end "ad3fee60-a0ea-4464-966c-bae3a964db79 end"

The child traversal is clean: ~bases_of(sender_type)~ reaches the ~product_type~ base that Beman Execution uses internally, and ~template_arguments_of~ on that base yields the child sender types at positions ~[2+]~, which are recursed into directly.

The node /classification/ is less clean, and for a concrete GCC16 reason.
The natural approach — ~template_arguments_of(sender_type)~ to get the tag at position 0, then ~identifier_of~ on it — does not work because ~template_arguments_of~ /throws/ when called on ~basic_sender~ specializations in GCC16.
Beman Execution's sender types are not directly interrogable as template specializations through the current P2996 implementation.

The child traversal side-steps this by going through the ~product_type~ base (~bases_of → type_of → template_arguments_of~), which /does/ work because ~product_type~ exposes its template arguments cleanly.
Classification cannot use the same path: identifying which template argument is the tag type requires either a splice (~[:r:]~) or knowing the argument's position, and GCC16 also cannot splice on ~std::meta::info~ values passed as ~consteval~ function parameters.

The workaround that works in GCC16 is ~std::meta::display_string_of(type)~, which returns the type's human-readable printed name as a ~string_view~, then string-searches for ~"just_t"~, ~"then_t"~, and ~"when_all_t"~.
This relies on implementation-defined output from ~display_string_of~ and would break if Beman Execution renamed its tag types or if a different compiler rendered names differently.
The minimum-position search rather than a plain ~find~ guards against false matches when a nested child tag name appears earlier in the full type string than the outermost tag.
It is explicitly a two-layer workaround for two distinct GCC16 limitations, not the intended long-term approach.

* Graph Translation via Applicatives

Once I possess a homogeneous C++ tree, I turn once again to Category Theory to render the Graphviz dot-language diagram.

Using the ~Applicative~ typeclass from the foundation—the same typeclass that drives the ~lift2~ and sequencing combinators in Phase 2's parser library—I constructed a ~string_writer~ Applicative.
The applicative and alternative interfaces allow me to map functionally over the returned string values.
While the Graphviz mapping is currently driven through a straightforward indexing iteration, the types align nicely allowing pure state accumulation.

#+transclude: [[orgit:~/src/steve-downey/schemepoc/schemepoc/::src/smd/smdscheme/sender/dump_scheme_plan.hpp::e0d42f47-3007-4d03-955a-2e94c9fad8b1]] :lines 2- :src cpp :end "e0d42f47-3007-4d03-955a-2e94c9fad8b1 end"

The ~dump_scheme_plan~ function encapsulates the compile-time reflection traversal into a ~constexpr~ lambda, cleanly executing the heavy `meta` machinery strictly during translation.

* Conclusion

Using the reflection bridge to populate my runtime topology completely removes the black-box nature of compile-time meta-programming. The C++26 reflection framework allows me to natively render the exact asynchronous execution pipeline (~std::execution~) constructed for any Scheme program.

Running the output directly through ~dot -Tpng -o plan.png~ grants immediate, visual feedback of how functional continuations map to low-level native scheduling operations.

* References

#+print_bibliography:
