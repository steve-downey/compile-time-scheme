include(GNUInstallDirs)

# The eleven example programs that used to live here, one per dead-ended
# front-end tree, are gone: both trees are dead-ended and A3 deletes them
# from trunk. A cl-equivalent example is real, positive work, tracked as
# docs/backlog/BL-0005-export-live-trees-and-turn-testinstall-green.md, not
# this step.
